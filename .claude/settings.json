{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "out=$(mktemp -d); log=$(zine release --output \"$out\" --force 2>&1); rc=$?; rm -rf \"$out\"; if [ $rc -ne 0 ]; then printf 'zine release failed:\\n%s\\n' \"$log\" >&2; exit 2; fi",
            "statusMessage": "zine release"
          }
        ]
      }
    ]
  }
}
